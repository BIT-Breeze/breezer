<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="post">
	<insert id="insert" parameterType="postvo">
		<![CDATA[
		  	INSERT INTO TB_POST (userId, photo, content, location, category, price, score, tourIdx, postDateTime, tripDateTime )
			VALUES (
					#{userId },
					#{photo },
					#{content },
					#{location },
					#{category },
					#{price },
					#{score },
					#{tourIdx },
					now(),
					#{tripDateTime }
					)
		 
		]]>	
	</insert>
</mapper>
