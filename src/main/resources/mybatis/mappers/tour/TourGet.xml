<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="tourget">
	<select id="gettours" parameterType="string" resultType="tourvo">
		<![CDATA[
			select idx, title, startDate from tb_tour where userId = 'hongseok5@gmail.com';
			
		]]>		
	</select>

</mapper>
